# AI 应用开发进阶学习计划

> **双线结构：**
> - **通用进阶** — 任何 AI 应用开发工程师都该掌握的进阶知识，来源于各厂商官方文档、Datawhale 路线图、行业最佳实践
> - **定制进阶** — 针对你的具体目标（杭州/电商/接单/零经验转行）推导的个性化内容
>
> **前置：** 完成 `plan.md` 的 Month 1-2（Python 基础 + LLM API + Streamlit 入门）
> **使用方式：** 两条线并行推进，不需要按顺序读完。具体时间安排见文末「时间线整合」

---

# 第一部分：通用进阶知识

> 这部分不管你以后做电商、内容、数据还是其他方向，都用得上。

---

## G1 — Agent 核心原理深化

**目标：** 不止会调 API，能说清楚 Agent 内部到底怎么运作的。

### 1.1 Function Calling 的完整链路

面试高频题："LLM 是怎么做 function calling 的？"

需要能讲清楚：
1. 请求时如何声明 tools（JSON Schema 格式）
2. 模型返回的不是"执行了工具"而是"建议调用哪个工具 + 参数"
3. 你的代码拿到建议 → 实际执行 → 把结果塞回 messages → 再次调模型
4. 循环终止条件：模型不再返回 tool_calls / 达到 max_turns / 你主动打断

**实操：** 不用任何框架，用最原始的 `OpenAI().chat.completions.create()` 写一个完整的 Agent loop（≤100 行），彻底理解每一步。

### 1.2 Agent Loop 的三种范式

| 范式 | 流程 | 适合场景 | 代表 |
|------|------|---------|------|
| ReAct | 思考 → 行动 → 观察 → 循环 | 通用任务，单步决策清晰 | LangChain ReAct |
| Plan-then-Execute | 先完整规划，再逐步执行 | 复杂多步任务，有计划更高效 | Plan-and-Solve |
| Reflexion | 执行 → 自我评价 → 修正 → 重试 | 需要质量保证的任务 | Reflexion Agent |

**学习目标：** 用同一个任务（如"帮我查出今天纳斯达克涨幅前三并总结原因"）分别用三种范式实现，感受差异。

### 1.3 Prompt Engineering 进阶

告别"凭感觉写 prompt"，建立系统方法：

- **结构化 Prompt：** System / Context / Instruction / Examples / Constraints 五段法
- **Few-shot 的策略：** 给几个例子？给什么样的例子？正例 vs 反例？
- **Chain-of-Thought 的触发时机：** 何时需要"让我们一步一步思考"？
- **Prompt 版本管理：** 把 prompt 当代码对待 —— 存 Git、写测试、做 diff

**实操：** 把你现有项目里的 prompt 全抽出来整理一遍，标准化结构。

### 1.4 LLM 选型与成本优化

| 场景 | 推荐模型 | 原因 |
|------|---------|------|
| 简单分类/提取 | DeepSeek V4 / Qwen-3B | 便宜够用 |
| 复杂推理 | DeepSeek Reasoner / Claude Opus 4.7 | 质量优先 |
| 多模态 | Claude Sonnet 4.6 / Qwen-VL | 图文理解 |
| 超长上下文 | Gemini 2.5 Flash (1M) | 窗口最大 |
| 实时/流式 | DeepSeek Chat / Claude Haiku | 延迟低 |

**核心原则：** 不要所有任务用同一个模型。根据任务复杂度分级路由，能省 60-80% 成本。

---

## G2 — Agent 工程化

**目标：** 能设计一个可维护、可扩展的 Agent 系统，不是一次性脚本。

### 2.1 Skills vs Tools 体系

这是 2026 年 Agent 设计的核心范式转变：

| | Tool | Skill |
|------|------|------|
| 粒度 | 单一操作 | 完整工作流 |
| 包含 | 接口 + schema | prompt + tools + 流程 + 测试 |
| 复用 | 跨场景直接调用 | 按场景组合 tools |
| 例子 | `search_web(query)` | 翻译 Skill = 语言检测 tool + 翻译 tool + 润色 tool + 质量检查 tool |

**实操：** 把你现有的 `translator.py` 和 `polish_app.py` 打包成 2 个标准 Skill（含 prompt + tools 声明 + 输入/输出 schema）。

### 2.2 Agent Harness 设计模式

以 **Claude Code** 为标杆拆解（因为你每天都在用）：

| 模块 | Claude Code 怎么做 | 你要学到什么 |
|------|-------------------|-------------|
| 工具注册 | 声明式工具定义，自动发现 `.claude/tools/` | 工具的热插拔模式 |
| 权限系统 | allow / deny / ask 三级，支持通配符匹配 | 权限中间件设计 |
| 状态管理 | 会话级上下文 + 项目级 Memory | 短期 vs 长期状态分离 |
| 子任务 | spawn subagent，独立上下文，返回摘要 | 子任务的生命周期管理 |
| 日志 | 每步 tool call 都有输入/输出/耗时 | Trace 日志的结构设计 |

**产出：** 画一张 Claude Code 的模块架构图 + 写 500 字分析（面试直接当作品集素材）。

参考项目对比学习：
- **Claude Code** — 编程 Agent 标杆，最成熟的权限/子任务设计
- **OpenClaw** — 个人常驻 Agent，学习持久化和后台运行模式
- **Hermes** — 学习 Agent 状态图 (State Machine) 的设计

### 2.3 MCP 协议深入

你计划 W6 学 MCP，进阶部分补充：

- **MCP 的三个传输层：** stdio（本地进程）、SSE/HTTP（远程服务）、Streamable HTTP
- **资源 (Resources) vs 工具 (Tools) vs 提示 (Prompts)：** MCP 不只是工具调用
- **自己写一个 MCP Server：** 比你用别人的 Server 更有价值
- **MCP 的局限：** 不适合长连接、不适合流式传输大量数据、没有内置权限

**实操：** 把你 battle.py 里的模型调用封装成 MCP Server，让 Claude Code 能通过 MCP 调用你的多模型对比能力。

### 2.4 多 Agent 协调模式

| 模式 | 结构 | 适用场景 | 坑 |
|------|------|---------|---|
| Supervisor | 一个主管路由给多个 Worker | 任务类型明确不同的场景 | 主管本身成为瓶颈 |
| Graph (LangGraph) | 有向图，节点=Agent，边=条件 | 流程固定的多步协作 | 图太复杂时难调试 |
| Swarm | 无中心，Agent 之间直接 handoff | 对话场景，不确定需要哪个 Agent | 可能形成无限 handoff 循环 |
| Hierarchical | 树形，上级分解任务给下级 | 复杂任务分解 | 层级深时延迟高 |

**关键教训（来自文章和社区）：**
- 永远给每个 Agent 设"停止条件" — 不能无休止调用
- 多 Agent 不等于更好，很多场景单 Agent + 多工具就够了
- 多 Agent 的调试成本是单 Agent 的 3-5 倍

**实操：** 从你的项目出发，用 LangGraph 做一个"调研 → 写作 → 审校"三 Agent Pipeline。

---

## G3 — Agent 生产化

**目标：** 能说清楚"我的 Agent 怎么保证质量、怎么监控、怎么不出事故"。

### 3.1 可观测性

| 层级 | 看什么 | 工具 |
|------|--------|------|
| LLM 调用层 | 每次 API 调用的输入/输出/耗时/Token/错误 | LangFuse / LangSmith |
| Agent 决策层 | Agent 每一步推理了什么、为什么选了这个工具 | Trace 里的 thought 字段 |
| 用户行为层 | 用户做了什么操作、在哪里卡住了 | 埋点 + Session Replay |
| 成本层 | 每次请求花了多少钱、哪个环节最烧钱 | Token × 单价 自动计算 |

**实操：** 为你的 `battle.py` 接入 LangFuse，能看到每次对比的完整 Trace + Token 费用。

### 3.2 评测体系

面试必问题："你怎么评测你的 Agent 好不好？"

```
评测金字塔：
  L3: 线上真实用户反馈（A/B test、满意度、留存）
  L2: 固定测试集回归（Golden Dataset，每次改动跑一遍）
  L1: 单次人工验证（开发时随手测）
```

**实操：**
- 为 `battle.py` 建 20+ 条固定测试（10 条翻译 + 5 条代码 + 5 条常识推理）
- 每次改 prompt 后跑一遍，看哪些 case 变差了

### 3.3 安全与 Guardrails

| 威胁 | 攻击方式 | 防御 |
|------|---------|------|
| Prompt Injection | 用户输入里夹带"忽略以上指令..." | 用户输入不进 system prompt，用独立消息块 |
| 工具滥用 | Agent 被诱导执行危险操作 | 工具权限分级 + 危险操作人工确认 |
| 数据泄露 | Agent 把内部 prompt 输出给用户 | 输出过滤 + 敏感词检测 |
| Token 耗尽 | 恶意超长输入导致巨额费用 | 输入长度限制 + 单用户配额 |

**实操：** 给你的翻译 Agent 加一个 Input Guard（检测注入关键词 → 拒绝 → 记录告警）。

### 3.4 部署与运维

- **开发态 vs 生产态：** Streamlit 适合 demo，FastAPI + 队列 + Worker 适合生产
- **冷启动问题：** Agent 启动时加载 tools/skills/memory 的开销
- **模型 fallback：** 主模型挂了自动切换到备选模型
- **速率限制：** 保护自己不被打爆，也保护 API 不被限流

---

# 第二部分：定制进阶知识

> 以下内容基于你的具体目标推导：杭州 AI 应用工程师 + 电商方向 + 学习期间接单 + 零经验转行。

---

## T1 — 阿里系 + 电商 AI 生态

**为什么这很重要：** 杭州的 AI 岗位 60%+ 与阿里生态有关。面试时你对 Qwen/百炼/通义的熟悉程度直接影响竞争力。

### 1.1 Qwen 模型家族深入

| 模型 | 定位 | 你的使用场景 |
|------|------|-------------|
| Qwen3-8B | 轻量通用，本地可跑 | 简单分类/提取，省钱 |
| Qwen3-72B | 主力通用模型 | 翻译、摘要、客服对话 |
| Qwen3-235B | 旗舰推理 | 复杂分析、多步推理 |
| Qwen-VL | 多模态 | 商品图片理解、OCR |
| Qwen-Agent | 阿里官方 Agent 框架 | 参考阿里系 Agent 设计范式 |

**实操：** W2 Day 2 接入 Qwen API 后，别止于跑通，专门做一次「DeepSeek vs Qwen 同任务对比分析」——记录两个模型在翻译/推理/代码场景的行为差异。这是面试宝藏素材。

### 1.2 阿里百炼平台

集成了 Qwen 全系列 + RAG + Agent 能力的一站式平台。

**需要了解：**
- DashScope SDK 的使用（你 W2 就要用）
- 百炼的应用模板（客服、问答、搜索）——理解阿里认为 AI 应该怎样产品化
- 百炼 vs Dify 的差异（一个是阿里生态，一个是通用）

**实操：** W3-W4 期间用百炼做一个"电商商品智能问答 Demo"（输入商品描述 → 回答用户问题 → 推荐相关商品）。

### 1.3 电商 AI 场景地图

杭州电商公司最常见的 AI 需求，按优先级排序：

| 场景 | 难度 | 技术点 | 变现潜力 |
|------|------|--------|---------|
| 智能客服（售前咨询） | ⭐⭐ | RAG + FAQ 匹配 + 意图识别 | 高 — 每个电商公司都需要 |
| 商品描述生成 | ⭐ | Prompt + 商品属性 → 多语言描述 | 中 — 可模板化接单 |
| 评论分析/舆情监控 | ⭐⭐ | 情感分析 + 关键意见抽取 | 中 — 数据服务 |
| 选品/比价 Agent | ⭐⭐⭐ | 浏览器 Agent + 数据聚合 | 高 — 决策类 AI |
| 个性化推荐 | ⭐⭐⭐⭐ | 用户画像 + 向量检索 + 排序 | 高 — 需要算法背景 |
| 供应链/库存预测 | ⭐⭐⭐⭐ | 时序预测 + 多源数据 | 中 — 偏数据分析 |

**建议：** 聚焦智能客服 + 商品描述生成，这是最容易出成果的两个方向，面试时能讲出完整故事。

### 1.4 杭州电商 AI 技术栈速查

```
淘宝/天猫生态:
  大模型 → 通义千问 (Qwen) → 百炼平台
  搜索/推荐 → 阿里云 Elasticsearch / Hologres
  数据分析 → DataWorks / MaxCompute
  部署 → 阿里云 SAE / ACK (K8s)
  
跨境电商:
  多语言 → DeepSeek V4 + Qwen (中英日韩)
  多平台 → 亚马逊/速卖通/Temu API 对接
  ERP → 聚水潭/店小蜜 等平台的 AI 化改造
```

---

## T2 — 接单与产品化

**为什么这很重要：** plan.md 明确写了"学习过程中通过接单/卖模板产生收入"。这不是副业，是证明你技能可商业化的面试证据。

### 2.1 AI 服务的可售卖形态

| 形态 | 例子 | 价格区间 | 复用性 |
|------|------|---------|--------|
| 一次性脚本 | "帮我写个批量翻译 Excel 的脚本" | ¥200-800 | 低 |
| Streamlit 工具 | "做一个多模型对比工具" | ¥500-2000 | 中 |
| API 服务 | "接入我的系统，按调用量计费" | ¥0.01-0.1/次 | 高 |
| 模板/插件 | "电商客服 AI 模板，直接部署" | ¥500-3000/套 | 高 |
| 定制 Agent | "为我公司定制的选品 Agent" | ¥3000-15000 | 低 |

**你的第一阶段可售品（基于现有技能）：**
1. **多语言翻译 API** — translator.py 改 API 化（W3 可做）
2. **邮件润色模板** — polish_app.py 打包成可部署模板（W3 可做）
3. **多模型对比评估报告** — battle.py 改成一键生成对比报告（W4 可做）

**接单渠道：**
- 闲鱼 / 淘宝（搜索"AI 开发"、"Python 脚本定制"）
- 猪八戒 / 程序员客栈
- 知乎 / 小红书（发案例 → 引流私域）
- AI 社群 / 微信群

### 2.2 定价与交付

| 步骤 | 做什么 | 避坑 |
|------|--------|------|
| 报价 | 按工时 × 你的时薪（初学阶段 ¥50-80/h）+ API 成本 × 1.5 | 别报太低 — 低了对方觉得不值钱，你也亏 |
| 定金 | 50% 预付，不付不动手 | 防跑单 |
| 交付物 | 代码 + 部署文档 + 5 分钟使用视频 | 别只给代码，客户不会跑 |
| 售后 | 7 天免费修 bug，超期按次收费 | 防止无限白嫖 |

### 2.3 从 Demo 到产品

你现在的 Streamlit 项目是 demo。产品化的差距：

| 维度 | Demo | 产品 |
|------|------|------|
| 错误处理 | `try/except` 打印错误 | 用户友好的错误提示 + 自动重试 + 告警 |
| 配置 | 硬编码 | 环境变量 + 配置面板 |
| 部署 | `streamlit run` 本地 | Docker + 云服务器 + 域名 |
| 认证 | 无 | 至少简单的密码保护 |
| 日志 | 无 | 每次调用的 Trace + 错误日志 |
| 收费 | 无 | API 次数限制 + 付费解锁 |

**实操（W5-W7）：** 挑你的一个 Streamlit 项目走完产品化全流程 —— 加上错误提示、Docker 打包、部署到阿里云 SAE、加访问密码。

---

## T3 — 求职靶向准备

**为什么这很重要：** 金九银十只有 3 个多月了。求职准备现在就可以开始穿插，不要等到 M4 才临时抱佛脚。

### 3.1 杭州 AI 公司图谱

| 类型 | 公司举例 | AI 岗位特点 | 技术栈偏好 |
|------|---------|-----------|-----------|
| 电商平台 | 阿里、网易严选、得物 | 重业务理解，AI + 电商场景 | Qwen、阿里云全家桶 |
| 跨境电商 | 拼多多 Temu、SHEIN、安克创新 | 多语言 AI、智能选品、客服 | DeepSeek/Qwen、MCP |
| AI 创业公司 | 杭州众多 MCP/Agent 相关创业 | 追求新技术，快速迭代 | LangChain、Agent 框架 |
| SaaS 公司 | 有赞、微盟 | AI 功能嵌入现有产品 | 工程化能力强优先 |
| 传统企业数字化 | 吉利、海康、大华 | AI 赋能业务，节奏较慢 | 稳定性 > 前沿性 |

**建议重点投递：** 跨境电商公司的 AI 岗位（你的翻译/多语言项目直接对口）+ AI 创业公司（技术栈匹配度高）。

### 3.2 零经验转行的简历策略

你最大的挑战不是"不会"，而是"没有工作经历证明你会"。

**策略：项目即简历：**

- 每个项目 = 一个故事：遇到了什么问题 → 怎么设计 → 怎么做 → 效果如何
- 给每个项目写一段"面试讲述词"（200 字内），能在一分钟内说清楚这个项目
- 用数字说话："对比了 4 个模型的翻译质量，Token 成本降低 40%"

**简历上不要写的：**
- "学习能力强" — 无法证明
- "精通 Python" — 你才学了两周
- 培训班风格的项目（天气查询、todo list）

**简历上要突出的：**
- 独立从零用 API 构建了 X 个 AI 工具（translator、battle、polish）
- 能用 Streamlit 快速交付 Demo（2-3 天出可演示版本）
- 对模型差异有真实体验（Chat vs Reasoner，不同场景的选型判断）
- 全程用 Claude Code 辅助开发（证明 AI 编程工具的使用能力）

### 3.3 面试项目讲述法（STAR 变体）

```
背景：我注意到（具体问题/需求）
思路：我决定用（技术方案），因为（选型理由）
实现：我从零写了（具体做了什么），花了 X 天
踩坑：（1 个具体的、真实的坑）
效果：（可以量化的结果，最好是数字）
学到了：（1 条有深度的反思）
```

**现在就可以开始：** 每周写完项目后，按照这个模板写一段 200 字的面试讲述词，存到 `progress.md`。

---

## T4 — 现有项目升级路线

**为什么这很重要：** 你现在已有 4 个项目。升级已有项目的投入产出比比重新做一个高得多。而且面试时可以讲"我是如何一步步把一个简单工具迭代成系统的"——这是高级工程师的思维。

### 4.1 translator.py → 多语言电商客服

```
当前状态：单次翻译，支持批量文件
    ↓ W3: 加语言检测 → 自动识别输入语言
    ↓ W4: 加 RAG（FAQ 库）→ 不只是翻译，能回答常见问题
    ↓ W5: 加意图识别 → "这个多少钱" vs "能发什么快递" 分别处理
    ↓ W6: 包装成 Skill → 可在其他 Agent 中调用
    ↓ W7: FastAPI + Docker → 可对外提供 API 服务

最终产品：一个支持中英日韩、能查 FAQ、能识别意图的电商客服 Agent
面试时：从"一个翻译脚本"开始讲，展示完整迭代过程
```

### 4.2 battle.py → 模型评测平台

```
当前状态：手动选模型 vs 模型对比，显示耗时和 Token
    ↓ W3: 加 Qwen → 3 模型对比（Chat / Reasoner / Qwen）
    ↓ W4: 建固定测试集 → 10 条翻译 + 5 条代码 + 5 条常识
    ↓ W5: 跑回归 → 每次改 prompt 后自动跑全部 case
    ↓ W6: 接入 LangFuse → 每次对比有完整 Trace
    ↓ W7-W8: 历史趋势 → "这个 prompt 改完后，翻译质量从 4.2 升到 4.5"

最终产品：一个能做回归测试、可视化模型质量趋势的评测平台
面试时：展示评测方法论，证明你知道怎么判断 Agent 好不好
```

### 4.3 polish_app.py → 内容 Agent 管线

```
当前状态：单次邮件润色，支持风格选择
    ↓ W4: 加多轮对话 → 润色 → 用户反馈 → 二次润色
    ↓ W5: 加智能配图 → 根据邮件内容生图提示词
    ↓ W6: 加多语言 → 中英文互译 + 润色
    ↓ W7: 打包成 Skill → 调研 + 写作 + 润色 + 审校 四步 Pipeline

最终产品：调研 → 写作 → 润色 → 审校的完整内容 Agent 管线
面试时：展示"从单功能工具进化到多 Agent 协作管线"的架构思维
```

---

# 时间线整合

通用进阶（G）和定制进阶（T）如何跟入职计划（plan.md）并行推进：

| 周 | 入职计划主线 | 通用进阶（穿插） | 定制进阶（穿插） |
|----|-----------|---------------|---------------|
| **W4** RAG 基础 | LlamaIndex + ChromaDB | G1.3 Prompt 标准化 | T1.2 百炼电商问答 Demo |
| **W5** LangChain 入门 | LCEL + Chain | G1.1 手写 Agent Loop | T4.1 翻译加语言检测 + FAQ |
| **W6** MCP + LangGraph | 协议 + 状态图 | G2.3 写 MCP Server + G2.2 Claude Code 拆解 | T4.2 battle 建测试集 |
| **W7** RAG 进阶 | RAGFlow / Milvus | G2.1 Skills 打包 | T4.3 润色改多轮对话 + 配图 |
| **W8** Dify 平台 | 用 Dify 搭一个完整应用 | G3.1 可观测性接入 | T2.3 一个项目走完产品化流程 |
| **W9** FastAPI 入门 | 把 Streamlit 改成 API | G3.3 安全 Guardrails | T4 各项目收尾 → 面试故事 |
| **W10** CrewAI 多 Agent | Multi-Agent 协作 | G2.4 多 Agent 协调 | T3 开始整理简历 + 面试词 |
| **W11** DSPy | Prompt 自动优化 | G1.2 Agent 范式对比 | T3 投递 + 模拟面试 |
| **W12** 项目深化 | 垂直领域项目 | G3.2 正式建立评测体系 | T3 持续面试 |
| **W13-W16** 面试 + 复盘 | — | — | 面试反馈 → 查漏补缺 → 再面 |
| **入职后** | — | G2.2 自建 Harness、G3.4 部署运维 | T1.3 电商 Agent 深度、T2 持续接单 |

**使用原则：**
1. **以入职计划为主线** — M1-M4 的产出目标不能变，进阶是叠加不是替代
2. **每次只选 1 个进阶主题** — 不要同时开 G2.2 和 T1.2，一个一个来
3. **做完比看完重要** — 进阶计划里的"实操"你不需要全做，挑最有感觉的 2-3 个深入做
4. **面试前 2 周暂停新增学习** — 集中精力打磨作品集和面试词
